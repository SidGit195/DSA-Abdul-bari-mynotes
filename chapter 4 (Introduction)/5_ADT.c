//ADT   ---> Abstract Data Type

//Data-Type ??
//(1)Representation of Data
//(2)Operation on Data

// append = add (something) to the end of a written document.
// Add(element) = append(element)
//
// insert = to put something into something or between two things
// Add(index, ele.) = Insert(index, ele.)       //element
// Remove(index)
// Set(index, ele.) = Replace(index, ele.)
// get(index)
// search(key) = contains(key)
// sort()


