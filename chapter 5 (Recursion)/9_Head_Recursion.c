//(2)Head Recursion :- Head Recursion means the function doesn't have to process or perform any operation at the time of calling; 
//it has to do everything only at the time of returning.

// Ex:-
// void fun(int n){
//     if(n>0){
//         fun(n-1);
//         printf("%d", n);
//     }
// }
